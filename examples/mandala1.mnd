// mandala1.mnd
// A simple symmetric mandala with 8 identical petals.
// Exercises: brush declaration, radial_repeat, named draw.

brush petal {
    radius: 40.0,
    angle: 45.0,
    color: #FF4500
}

let segments : int = 8;
let step : angle = 45.0;

radial_repeat(segments) {
    draw petal;
}